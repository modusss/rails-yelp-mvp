<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: 1..5, prompt: "Define your rate", selected: 3 %>
  <%= f.submit "add a review", class: "btn btn-primary" %>
<% end %>
